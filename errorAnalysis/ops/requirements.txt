# Dependencies for the project-state automation (errorAnalysis/ops).
#
# The weekly report generator (weekly_report.py) and the synthesizer
# (synthesize_state.py) are pure stdlib + the `git`/`gh` CLIs, so they have NO
# pip dependencies and run unchanged in GitHub Actions. The only thing that
# needs a package is local meeting transcription.

# Local Whisper transcription (transcribe_meeting.py). faster-whisper is the
# CTranslate2 backend: fast on CPU, no GPU required for small/base models.
faster-whisper>=1.0.0

# Optional: faster-whisper can read most audio/video containers directly via
# bundled ffmpeg bindings, but having a system ffmpeg available is the most
# robust path for arbitrary meeting recordings (e.g. .m4a from QuickTime).
# Install ffmpeg via your OS package manager (brew install ffmpeg) if needed.
